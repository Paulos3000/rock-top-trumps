import React from 'react'

const Posts = ({ rootPage }) => (
   <div>
      <h1>Posts Page</h1>
      <p>This is the Posts page. It makes no API calls.</p>
      <p>rootPage: {rootPage}</p>
   </div>
)

export default Posts;

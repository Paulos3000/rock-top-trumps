import React from 'react'

const Notes = () => (
   <div>
      <ul>
         <li>#11, Normalising State Shape: tricky, but it works. Maybe go over again.</li>
         <li>#12, Wrapping dispatch() to log functions: basically a manual Redux Devtools. Maybe check out console.group() api, plus console.log styling.</li>
      </ul>

      <hr/>

      <ul>
         <li>#13, Redux: Adding a fake backend. </li>
      </ul>
   </div>
)

export default Notes
